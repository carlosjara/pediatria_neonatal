# WHO 2006 LMS reference data

These CSV files were extracted from the WHO `anthro` R package version 1.1.0 (`sysdata.rda`) for offline use in the iOS app. The package is published on CRAN and maintained by contributors including the World Health Organization.

Runtime code reads these CSV files using the Python standard library; R, pandas and rdata are not required by the app.
